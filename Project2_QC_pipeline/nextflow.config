process {
  executor = 'awsbatch'
  queue = 'QC_job'   #AWS Job queue name
  container = 'ubuntu:20.04'
}

aws {
  region = 'eu-north-1'  // #Set the region to the same as both S3 and AWS batch.
}

workDir = 's3://qc-nextflow-bucket/work' #s3 bucket
